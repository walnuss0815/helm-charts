apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-kiosk.fullname" . }}
  labels:
    {{- include "immich-kiosk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-kiosk.selectorLabels" . | nindent 4 }}

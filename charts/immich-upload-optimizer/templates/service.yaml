apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-upload-optimizer.fullname" . }}
  labels:
    {{- include "immich-upload-optimizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-upload-optimizer.selectorLabels" . | nindent 4 }}

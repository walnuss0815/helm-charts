apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "immich.configScriptsName" . }}
  labels:
    {{- include "immich.labels" . | nindent 4 }}
data:
  substitute-config.js: |
    {{- .Files.Get "scripts/substitute-config.js" | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "immich.fullname" . }}-test-connection"
  labels:
    {{- include "immich.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  automountServiceAccountToken: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    runAsGroup: 65534
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: wget
      image: busybox:1.36.1
      command: ['wget']
      args:
        - '-q'
        - '-O'
        - '/dev/null'
        - '-T'
        - '10'
        - '{{ include "immich.fullname" . }}:{{ .Values.server.service.port }}/api/server/ping'
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
  restartPolicy: Never

{{- with .Values.persistence.data }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "odoo.fullname" $ }}-data
  labels:
    {{- include "odoo.labels" $ | nindent 4 }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
  {{- if not (empty .accessModes) }}
  {{- range .accessModes }}
    - {{ . | quote }}
  {{- end }}
  {{- else }}
    - {{ .accessMode | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .size | quote }}
  {{- if not (eq .storageClass "-") }}
  storageClassName: {{ .storageClass }}
  {{- end }}
{{- end }}

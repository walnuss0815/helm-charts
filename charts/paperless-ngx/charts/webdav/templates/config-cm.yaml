apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webdav.fullname" . }}-config
  labels:
    {{- include "webdav.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- toYaml .Values.config | nindent 4 }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "paperless-ngx.fullname" $ }}-scripts
  labels:
    {{- include "paperless-ngx.labels" $ | nindent 4 }}
data:
  {{- (.Files.Glob "scripts/**.sh").AsConfig | nindent 2 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "ultrafeeder.fullname" . }}
  labels:
    {{- include "ultrafeeder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ultrafeeder.selectorLabels" . | nindent 4 }}

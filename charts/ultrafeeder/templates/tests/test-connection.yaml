apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ultrafeeder.fullname" . }}-test-connection"
  labels:
    {{- include "ultrafeeder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ultrafeeder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
